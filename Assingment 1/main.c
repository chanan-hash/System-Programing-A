#include <stdio.h>
#include "NumClass.h"

// TODO add here a simple check for each function
int main()
{
    // int x, y, i;
    // scanf("%d%d",&a,&b); // Scaning the numbers for the  user
    printf("%d", isPrime(2));
}