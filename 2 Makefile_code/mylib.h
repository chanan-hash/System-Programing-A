#ifndef _MYLIB_H_
#define _MYLIB_H_

    // a constant definition exported by library:
    #define MAX_FOO  20

    
    // a function prototype for a function exported by library:
    int 	foo(float y, float z);   // a very bad function name
    double  bar(double x);
    
    
    #endif
